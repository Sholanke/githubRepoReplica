import React from 'react'
import "./index.scss"

export default function MiniModal({children}) {
    return (
        <div className="github_mini_modal">
            {children}
        </div>
    )
}
